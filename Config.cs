/*
using System;
using Newtonsoft.Json;
using System.IO;

class Config
{
    static void Config()
    {
         string ConfigPath = "test.json";

         string Content = File.ReadAllText(jsonFilePath);

         dynamic Data = JsonConvert.DeserializeObject(jsonContent);

         string Value = jsonData.hi;

         Console.WriteLine(Value);
    }
}
*/